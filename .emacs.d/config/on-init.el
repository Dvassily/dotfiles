					; init settings
;; no startup msg  
(setq inhibit-startup-message t)

;; Enables yasnippet on all buffers
(yas-global-mode 1)

;; Disable toolbar
(tool-bar-mode 0)

;; Disable menubar
(menu-bar-mode 0)

;; Disable scrollbar
(scroll-bar-mode 0)

;; scratch buffer content
(setq initial-scratch-message ";; Lisp evaluation : \n\n")

;; Color in compilation mode
(require 'ansi-color)


